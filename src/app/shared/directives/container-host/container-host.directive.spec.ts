import { ContainerHostDirective } from './container-host.directive';

describe('ContainerHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerHostDirective();
    expect(directive).toBeTruthy();
  });
});
